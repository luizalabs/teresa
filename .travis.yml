language: go

go:
  - 1.8
  - tip

jobs:
  include:
    - stage: helm lint
      before_script:
        - ./.helm-test.sh download
      script:
        - ./.helm-test.sh lint
    - stage: test
      script:
         - ./.tests-run.sh
      after_success:
        - bash <(curl -s https://codecov.io/bashl)
    - stage: build docker image
      script:
        - ./.ci-docker.sh
